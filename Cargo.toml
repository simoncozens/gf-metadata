[workspace.dependencies]

# external deps
protobuf = "3.7.2"
home = "0.5.11"
regex = "1.11.1"
walkdir = "2.5.0"
clap = { version = "4.5.37", features = ["derive"] }

# dev dependencies

[workspace]
resolver = "2"

members = [
    "make_embedding",
    "gf-metadata",
    "make_test_images",
    "read-gf-metadata",
]

